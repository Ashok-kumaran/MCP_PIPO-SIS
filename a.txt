1) Create a new CPI iFlow called "rt1"in package "mcppipo" with an HTTPS sender that accepts JSON input, converts it to XML, and sends the final XML file to an SFTP server.


2)Create a new CPI iFlow called "rt2"in package "mcppipo" that triggers daily using a Timer event, collects all error artifacts from that day, and sends them as an attachment or summary to a recipient email address.


3)Create a new CPI iFlow called "rt3"in package "mcppipo" that picks up a file from an SFTP folder and sends it to a REST API endpoint.


4) Create a new CPI iFlow called "rt4"in package "mcppipo" that receives JSON via HTTPS, performs value mapping, and sends the result to a REST API


5)Create a new CPI iFlow called "rt5"in package "mcppipo" that reads incoming emails and saves attachments to an SFTP folder



1.  Create a CPI iFlow in the package "mcppipo" with the iFlow name "r6", where the scenario uses an 
HTTPS sender adapter that accepts JSON input. The iFlow should convert the JSON payload into XML, 
optionally enrich headers or properties during processing, prepare the XML for file delivery, 
and then send the final XML file to an SFTP server. The overall goal is to receive JSON over HTTPS, 
transform it to XML, and upload it to SFTP.

2. Create a CPI iFlow in the package "mcppipo" with the iFlow name "r7" where the scenario is triggered daily using 
a Timer event. The iFlow must collect all error artifacts generated during that day, compile them into 
a summary or attachment, and then send the result to a designated recipient using the Mail adapter. 
The overall goal is to automatically send a daily error report via email.

3. Create a CPI iFlow in the package "mcppipo" with the iFlow name "" where the scenario uses an SFTP sender adapter 
to pick up a file from a defined SFTP folder. During the processing step, the iFlow may optionally perform 
format adjustments or simple transformations before sending the file content to a REST API endpoint 
using a REST receiver adapter. The overall goal is to transfer a file from SFTP to a REST API.

4. Create a CPI iFlow in the package "mcppipo" with the iFlow name "" where the scenario uses an HTTPS sender adapter 
to receive JSON data. The iFlow should convert the JSON into XML if required, perform value mapping on 
relevant fields, and then convert the payload back into JSON if needed before forwarding 
the final transformed message to a REST API using a REST receiver adapter. The overall goal is to receive JSON, 
apply value mapping, and send the result to a REST API.

5. Create a CPI iFlow in the package "mcppipo" with the iFlow name "" where the scenario uses a Mail adapter 
to read incoming emails from a monitored inbox. The iFlow should extract all attachments found in those emails, 
process them as needed, and then save the resulting files into a specified SFTP folder using 
an SFTP receiver adapter. The overall goal is to collect email attachments and store them in SFTP.

